<% @articles.each do |article| %>
<article>
  <h3 class="text-center"><%= link_to article.title, article_path(article) %></h3>
    <%= article.content %>
    <p class="text-secondary mt-2"><%= article.author %></p>
  </article>
<% end %>
</div>
</div>
